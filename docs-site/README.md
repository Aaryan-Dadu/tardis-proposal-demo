# Approach-4 Gallery Site

This folder is generated as static HTML and can be published directly via GitHub Pages.

- `index.html` is created by `scripts/build_gallery.py`
- It links each generated notebook and its source config
- If no notebooks are available yet, it shows a placeholder message
